# RunPod
runpod>=1.6.0

# PyTorch - will be installed via base image
torch>=2.0.0
torchvision>=0.15.0

# Transformers for model loading
transformers>=4.35.0

# bb-models package (local install)
# Install with: pip install -e ../../packages/bb-models
# Or copy to Docker image

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# HTTP client
httpx>=0.24.0

# Training utilities
tqdm>=4.65.0
tensorboard>=2.14.0

# Optional: EMA
torch-ema>=0.3

# Cloud storage (for checkpoint upload)
boto3>=1.28.0  # AWS S3
google-cloud-storage>=2.10.0  # GCS (optional)
