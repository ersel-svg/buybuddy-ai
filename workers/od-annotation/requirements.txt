# OD Annotation Worker - Requirements
# Note: torch and torchvision are installed separately in Dockerfile with CUDA support

# RunPod SDK
runpod>=1.6.0

# Core ML Libraries
numpy>=1.24.0
scipy>=1.11.0
scikit-image>=0.21.0
opencv-python-headless>=4.8.0

# Image Processing
Pillow>=10.0.0
imageio>=2.31.0

# HTTP/Networking
httpx>=0.25.0
aiohttp>=3.9.0
requests>=2.31.0

# Hugging Face (Florence-2, SAM HF)
transformers>=4.38.0
accelerate>=0.26.0
safetensors>=0.4.0
huggingface_hub>=0.20.0

# Grounding DINO dependencies
supervision>=0.18.0

# Ultralytics (YOLOv8, RT-DETR) for Roboflow trained models
ultralytics>=8.1.0

# Roboflow RF-DETR (for RF-DETR training checkpoints)
rfdetr>=1.0.0

# ONNX for custom model support
onnx>=1.15.0
onnxruntime-gpu>=1.16.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.0
einops>=0.7.0

# Logging
loguru>=0.7.0
