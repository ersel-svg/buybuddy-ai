# torch and torchvision come from base image (pytorch:2.8.0-cuda12.8)
# DO NOT install via pip - causes CUDA binary mismatch

# RunPod serverless
runpod>=1.6.0

# HuggingFace transformers (for D-FINE, RT-DETR)
transformers>=4.45.0

# Ultralytics (for YOLO models)
ultralytics>=8.1.0

# Image processing
pillow>=10.0.0
numpy>=1.24.0

# HTTP client
httpx>=0.26.0

# Vision models
timm>=1.0.0
