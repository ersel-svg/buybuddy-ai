# torch and torchvision come from base image (pytorch:2.8.0-cuda12.8)
# DO NOT install via pip - causes CUDA binary mismatch

# RunPod serverless
runpod>=1.6.0

# HuggingFace transformers (for D-FINE, RT-DETR, Grounding DINO, OWL-ViT)
transformers>=4.45.0

# Ultralytics (for YOLO models)
ultralytics>=8.1.0

# Super-gradients (for YOLO-NAS)
# Note: super-gradients requires onnxruntime==1.13.1 specifically
onnxruntime==1.13.1
super-gradients>=3.5.0

# Image processing
pillow>=10.0.0
numpy>=1.24.0

# HTTP client
httpx>=0.26.0

# Vision models
timm>=1.0.0

# Additional HuggingFace utilities
accelerate>=0.25.0
safetensors>=0.4.0
